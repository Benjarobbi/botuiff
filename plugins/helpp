
local function run(msg, matches)
  local text = "[[This is help text, you can edit this with yours]]"
  return text
end

return {
  description = "help",
  usage = "!help",
  patterns = {
    "^!help$"
  }, 
  run = run 
}
